;; Define window basis

(defwindow dashboard
  :wm-ignore true
  :windowtype "normal"
  :anchor "center"
  :geometry (geometry
    :height "70%"
    :width "70%")
  (dashboard_layout)
)


(defwidget dashboard_layout []
  (label "hello")


)
